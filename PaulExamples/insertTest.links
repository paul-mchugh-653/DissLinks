var db = database "linkstry";

var people = table "people" with (name: String, age: nullable Int) from db;

fun main() {

	insert people values (name, age) [(name = "Mark", age = Nothing(0))]

}

main()
